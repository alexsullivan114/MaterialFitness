<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="peoples.materialfitness" >

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:name=".Core.MaterialFitnessApplication"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".RootScreen.RootActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WorkoutDetails.ActiveWorkoutDetailsActivity.ActiveWorkoutDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".WorkoutDetails.HistoricalWorkoutDetailsActivity.HistoricalWorkoutDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".FitnotesImport.FitnotesImporterActivity" />

    </application>

</manifest>
